# EmployeeManagementSystem

A command-line application that allows the user to add departments, roles, employees, view departments, roles, employees and update employee roles. Utilizes mySQL and Inquirer.